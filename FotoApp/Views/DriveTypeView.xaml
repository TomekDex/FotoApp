<UserControl x:Class="FotoApp.Views.DriveTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FotoApp.Views"
             xmlns:view="clr-namespace:FotoApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ItemsControl HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                      ItemTemplate="{StaticResource ButtonDriveType }"
                      ItemsPanel="{StaticResource ListPanel}" >
            <ItemsControl.ItemsSource>
                <Binding Path="Drive" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" />
            </ItemsControl.ItemsSource>
            
        </ItemsControl>
            
    </Grid>
</UserControl>
